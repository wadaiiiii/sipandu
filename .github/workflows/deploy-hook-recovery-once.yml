name: Check SiPANDU Deploy Hook Queue

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/deploy-hook-recovery-once.yml'

permissions:
  contents: read

jobs:
  inspect:
    name: Cek antrean deployment Vercel
    runs-on: ubuntu-latest
    env:
      VERCEL_SCOPE: andiseppewali-3458s-projects
      VERCEL_PROJECT: sipandumath
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

    steps:
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: List queued/building/error deployments
        shell: bash
        run: |
          set -euo pipefail
          vercel list "$VERCEL_PROJECT" --status QUEUED,BUILDING,INITIALIZING,ERROR --scope "$VERCEL_SCOPE" --token "$VERCEL_TOKEN" --yes
